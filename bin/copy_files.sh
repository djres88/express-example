#!/bin/bash
# TODO: write script that takes in a single arg - the dirname - and copies files from that dirname to an express project.
# Also TODO: add options for react (jsx/tsx) where json is sent to stores (i.e. rather than templating)